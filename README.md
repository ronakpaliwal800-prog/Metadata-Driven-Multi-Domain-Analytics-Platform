# ETL Pipeline Project

## Overview
This project is an **end-to-end ETL pipeline** built using Python, Pandas, and PostgreSQL. 
It extracts data from CSV files, transforms it, validates it, and loads it into a PostgreSQL database.

## Folder Structure
etl_pipeline/
│── data/
│ └── raw/
│ └── orders.csv
│── extract.py
│── transform.py
│── validation.py
│── load.py
│── main.py
│── config.py
│── requirements.txt
│── README.md
│── venv/